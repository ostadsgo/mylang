// collection will initialize with []
// based on type collection can be diffrent type
// Is it good to use this Int or int or Array[int] or Array[int] or array[type]?
// defualt behaviour of [] is create int 
// everything start with virbousity and then get cleaner and consise with the help of puncation.
// So you need to be less varbous and readable but still you need to be consise with symbols.
// and this is not good alwyse beacause you end up with creating multiple way of same thing.


// create array
var [] numbers = [1, 2, 3];

// empty array
var number = array(int, [1, 2, 3]);
[int] numbers; // another way

// specific way
var array[int] numbers;

// var numbers = [int];
// var number = array[int];
// var [int] numbers;  // empty array
// var numbers = Array(type=int);
