


fib :: (n: int) -> int {
    if n <= 1 {
        return 1;
    } else {
        return fib(n-1) + fib(n - 2);
    }
}
    
/* 0, 1, 1, 2, 3, 5, ... */
fib2 :: (n: int) -> int {
    a := 0;
    b := 1;
    for a < 100 {
        c = a + b;
        a = b;
        b = c;
    }
}
